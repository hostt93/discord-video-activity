services:
  - type: web
    name: discord-video-activity
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DISCORD_BOT_TOKEN
        sync: false
      - key: DISCORD_CLIENT_ID
        sync: false
      - key: ACTIVITY_COMMAND_NAME
        value: watch
